---
description: Archived — use plugin skill flutter-mcp-toolkit-custom-tools for AgentCallEntry dynamic registry authoring.
globs:
alwaysApply: false
---

# MCP Dynamic Tools (archived)

This Cursor rule is **retired**. Author dynamic tools with **`AgentCallEntry`** (or `mcpToolkitTool` / `mcpToolkitResource` bridges), not the removed `MCPCallEntry` type.

**Use instead:**

- Repo skill: `plugin/skills/flutter-mcp-toolkit-custom-tools/SKILL.md`
- Migration (before/after only): `plugin/skills/flutter-mcp-toolkit-intentcall-migration/SKILL.md`
- CLI: `flutter-mcp-toolkit migrate agent-entries`
- Discovery: `fmt_list_client_tools_and_resources` → invoke via `fmt_client_tool` / `fmt_client_resource`

After editing plugin skills, run `make sync-skills`.
