name: agentkit_mcp
description: MCP adapter for agentkit registry (dart_mcp publish bridge).
version: 0.1.0
publish_to: none
environment:
  sdk: ">=3.11.0 <4.0.0"
resolution: workspace

dependencies:
  agentkit_core:
  agentkit_schema:
  dart_mcp: ^0.5.0
  meta: ^1.17.0

dev_dependencies:
  flutter_mcp_toolkit_capability_kernel:
  lints: ^6.1.0
  test: ^1.31.1
