name: agentkit_platform
description: Platform emitters and sync for agentkit (web manifest, WebMCP JS, bootstrap).
version: 0.1.0
publish_to: none
environment:
  sdk: ">=3.11.0 <4.0.0"
resolution: workspace

dependencies:
  agentkit_core:
  agentkit_schema:
  meta: ^1.17.0
  path: ^1.9.1
  web: ^1.1.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
