> ⚠️ **Pre-release (0.1.x)** — Highly experimental. APIs may change without notice. Not for production. [Details](../../PRE_RELEASE.md).


# agentkit_schema

Wire types, `AgentResult`, validation, and `AgentArguments` for the agentkit stack.

```yaml
dependencies:
  agentkit_schema: ^0.1.0
```