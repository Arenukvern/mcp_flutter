> ⚠️ **Pre-release (0.1.x)** — Highly experimental. APIs may change without notice. Not for production. [Details](../../PRE_RELEASE.md).


# agentkit_mcp

MCP bridge: `McpPublishAdapter`, `ToolRegistration`, registry ↔ `dart_mcp` publish.

Depends on `agentkit_core` and `agentkit_schema`. Only agentkit package that imports `dart_mcp`.