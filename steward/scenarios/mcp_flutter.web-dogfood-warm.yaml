schema: steward/scenario-manifest/v1
repo: mcp_flutter
scenario: mcp_flutter.web-dogfood-warm
status: superseded
source:
  git: https://github.com/Arenukvern/mcp_flutter
  commit: 21c160a09bb2d7fe18993c8a703bb05d1cc64ff9
  steward_contract: steward.yaml
safe_first_probe: fmt.check.tool-prefix
required_actions:
  - fmt.check.tool-prefix
artifacts:
  - id: steward_contract
    kind: yaml
    path: steward.yaml
    required: true
    durability: input
  - id: tool_prefix_contract
    kind: text
    path: tool/contracts/expected_tool_surface.txt
    required: true
    durability: input
blocked_by: superseded by mcp_flutter.contract-status-smoke; this historical scenario never proved live WebMCP runtime dogfood
owner: flutter-mcp-toolkit
